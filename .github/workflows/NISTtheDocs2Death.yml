name: "Build Documentation"

on: [push, pull_request, delete]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: usnistgov/NISTtheDocs2Death@main
        with:
          pre-build-command: >
            which git
            || (
            (
            apt-get -yq update
            && apt-get -yq install git
            && rm -rf /var/lib/apt/lists/*
            )
            || (
            apk update --no-cache
            && apk add --no-cache git
            )
            )
            && git config --global --add safe.directory /github/workspace
            && sphinx-apidoc
                --output-dir=docs/source/generated/fipy
                --suffix=rst fipy
            && sphinx-apidoc
                --output-dir=docs/source/generated/examples
                --suffix=rst examples
            && sphinx-apidoc
                --output-dir=docs/source/generated/tutorial
                --suffix=rst docs/source/tutorial/package
