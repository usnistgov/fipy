# This file was adapted from what's generated by conda-smithy.

environment:

  matrix:
    - TARGET_ARCH: x64
      CONDA_PY: 27
      CONDA_INSTALL_LOCN: C:\\Miniconda-x64
      FIPY_SOLVERS: pysparse

#    - TARGET_ARCH: x64
#      CONDA_PY: 27
#      CONDA_INSTALL_LOCN: C:\\Miniconda-x64
#      FIPY_SOLVERS: pysparse
#      FIPY_INLINE: 1

#    - TARGET_ARCH: x86
#      CONDA_PY: 27
#      CONDA_INSTALL_LOCN: C:\\Miniconda
#      FIPY_SOLVERS: scipy

    - TARGET_ARCH: x64
      CONDA_PY: 27
      CONDA_INSTALL_LOCN: C:\\Miniconda-x64
      FIPY_SOLVERS: scipy

#    - TARGET_ARCH: x86
#      CONDA_PY: 36
#      CONDA_INSTALL_LOCN: C:\\Miniconda36
#      FIPY_SOLVERS: scipy

    - TARGET_ARCH: x64
      CONDA_PY: 36
      CONDA_INSTALL_LOCN: C:\\Miniconda36-x64
      FIPY_SOLVERS: scipy

# We always use a 64-bit machine, but can build x86 distributions
# with the TARGET_ARCH variable.
platform:
    - x64

install:
    # If there is a newer build queued for the same PR, cancel this one.
    - cmd: |
        powershell -Command "(New-Object Net.WebClient).DownloadFile('https://raw.githubusercontent.com/conda-forge/conda-forge-build-setup-feedstock/master/recipe/ff_ci_pr_build.py', 'ff_ci_pr_build.py')"
        ff_ci_pr_build -v --ci "appveyor" "%APPVEYOR_ACCOUNT_NAME%/%APPVEYOR_PROJECT_SLUG%" "%APPVEYOR_BUILD_NUMBER%" "%APPVEYOR_PULL_REQUEST_NUMBER%"
        del ff_ci_pr_build.py

    # Cywing's git breaks conda-build. (See https://github.com/conda-forge/conda-smithy-feedstock/pull/2.)
    - cmd: rmdir C:\cygwin /s /q

    # Add path, activate `conda` and update conda.
    - cmd: call %CONDA_INSTALL_LOCN%\Scripts\activate.bat
    - cmd: conda.exe update --yes --quiet conda

    - cmd: set PYTHONUNBUFFERED=1

    # Add our channels.
    - cmd: conda.exe config --set show_channel_urls true
    - cmd: conda.exe config --set always_yes yes
    - cmd: conda.exe config --set changeps1 no
    - cmd: conda.exe config --remove channels defaults
    - cmd: conda.exe config --add channels defaults
    - cmd: conda.exe config --add channels conda-forge

    # Configure the VM.
    - cmd: if "%TARGET_ARCH%" == "x64" conda.exe install --quiet --name root python fipy
    - cmd: if "%TARGET_ARCH%" == "x64" conda.exe uninstall --quiet fipy
    - cmd: echo "working directory is %cd%"
    # FIXME: fipy recipe on conda-forge doesn't have gmsh
    - ps: |
        $ErrorActionPreference = "Stop";
        echo "env..TARGET_ARCH  is $env:TARGET_ARCH"
        if ($env:TARGET_ARCH -eq "x64") {
            echo "working directory is $(get-location)"
            echo "getting gmsh"
            Invoke-WebRequest -Uri 'https://gmsh.info/bin/Windows/gmsh-4.3.0-Windows64.zip' -OutFile gmsh-4.3.0-Windows64.zip
            echo "got gmsh"
            echo "$(ls)"
            Add-Type -A 'System.IO.Compression.FileSystem'
            echo "unzipping gmsh"
            echo "$(ls)"
            [IO.Compression.ZipFile]::ExtractToDirectory('$(get-location)\gmsh-4.3.0-Windows64.zip', '$(get-location)')
            echo "unzipped gmsh"
            copy gmsh-4.3.0-Windows64\gmsh.exe $env:CONDA_INSTALL_LOCN\Scripts\
            echo "copied gmsh"
            echo "$(ls) $env:CONDA_INSTALL_LOCN\Scripts\"
        }
    - ps: $blockRDp = $true; iex ((new-object net.webclient).DownloadString('https://raw.githubusercontent.com/appveyor/ci/master/scripts/enable-rdp.ps1'))
    # comment

    - cmd: if "%TARGET_ARCH%" == "x86" conda.exe install --quiet --name root python numpy scipy matplotlib
    - cmd: if "%TARGET_ARCH%" == "x86" if "%CONDA_PY%" == "27" conda.exe install --quiet --name root mayavi weave
    - cmd: if "%TARGET_ARCH%" == "x86" if "%FIPY_SOLVERS%" == "pysparse" conda.exe install --quiet --name root pysparse

    - cmd: pip install future
    - cmd: pip install scikit-fmm

# Skip .NET project specific build phase.
build: off

test_script:
    - python setup.py egg_info
    - if !%FIPY_INLINE%==! python setup.py test 1> NUL 2>&1
    - python setup.py test
    - conda env export
# deploy_script:
#     - cmd: upload_or_check_non_existence .\recipe guyer --channel=main
