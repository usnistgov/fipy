import string
import time
import os
from ../../ import setup

##call some function in copy_script class

readin = open('../../examples/diffusion/mesh1D.py','r')
readinstr = readin.read()

##times = []
##time += time.time()
##find "mesh =", then find the next \n after that
##put in a times += time.time()
##find "for", then find the next \n after that
##put in a \t times += time.time()
##do the same for any "while" loops
##put in a times += time.time() at eof

## write new file to place for setup.py efficiency_test to find and \
##run for benchmarks, uploading results to Codespeed.


