parameters:
  platform: ''
  python.version: 3.9
  conda.packages: ''
  experimentalTemplate: false

steps:
  - bash: echo ${{ parameters.platform }}
    displayName: Echo 1

  - ${{ or(eq(parameters.platform, 'macos'), eq(parameters., 'ubuntu')) }}:
    - template: install_bash.yml
