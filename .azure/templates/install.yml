parameters:
- name: platform
  default: ''
- name: python.version
  default: 3.9
- name: conda.packages
  default: ''
- name: isBash
  default: false

steps:
  - bash: echo ${{ parameters.platform }}
    displayName: Echo 1

  - bash: echo $(isBash)
    displayName: Echo 2

  - ${{ isBash }}:
    - template: install_bash.yml
