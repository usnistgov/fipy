parameters:
- name: python.version
  default: 3.9
- name: conda.packages
  default: ''

steps:
  - template: install_bash.yml
    condition: or(eq(variables.platform, 'macos'), eq(variables.platform, 'ubuntu'))
