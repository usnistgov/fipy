parameters:
- name: python.version
  default: 3.9
- name: conda.packages
  default: ''

steps:
- template: install_bash.yml
  parameters:
    python.version: $(python.version)
    conda.packages: $(conda.packages)
